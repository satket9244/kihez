---
### 🛠 Professional Technical Specification

1. **Analyze current notification implementation**:
   - Locate the existing push notification handling code, likely in a service or receiver class that processes Firebase Cloud Messaging (FCM) or similar push services
   - Identify where `NotificationCompat.Builder` is used to construct notifications

2. **Modify notification layout to support dynamic text sizing**:
   - Update the notification builder to use `setStyle()` with `BigTextStyle` to enable expandable notifications:
     ```kotlin
     val bigTextStyle = NotificationCompat.BigTextStyle()
         .bigText(longTextFromPushMessage)
         .setBigContentTitle("Notification Title")
     
     val notification = NotificationCompat.Builder(context, CHANNEL_ID)
         .setSmallIcon(R.drawable.ic_notification)
         .setStyle(bigTextStyle)
         // other properties
         .build()
     ```
   - Ensure that `bigText()` is used instead of `setContentText()` for dynamic sizing
   - Implement proper notification channel setup with high priority for better text rendering

3. **Test notification rendering**:
   - Verify that both short and long text messages display correctly
   - Confirm that the notification expands to show full text when the message is long
   - Test on various Android versions to ensure compatibility

### 📊 Current Project Context

The project structure only contains the AndroidManifest.xml file, indicating this is a minimal or new project. Since push notifications are handled through Android's notification system, the implementation will likely involve:
- A service extending `FirebaseMessagingService` or similar push handler
- Notification builder using `NotificationCompat.Builder`
- The fix requires modifying how notifications are constructed to support expandable layouts using `BigTextStyle` for dynamic text sizing

The solution involves updating the notification creation logic to use the appropriate BigTextStyle for expandable notifications that adjust to text length.

---